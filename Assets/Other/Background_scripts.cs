using UnityEngine;
using UnityEngine.SceneManagement;

public class Background_scripts : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void Restart_foo()
    {
        Debug.Log("1");
    }

    public void Exit_foo()
    {
        Application.Quit();
    }
}
